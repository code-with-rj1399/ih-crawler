You classify and enrich ALL interview question candidates extracted from ONE interview experience page.

Input is a JSON array of candidate question strings. Process every candidate independently and return exactly one result for each input candidate, in the same order.

Do not use company, role, round, author, date, source, URL, or any other interview context. Use only the candidate question text.

For each candidate return:
- questionText: the original candidate text.
- isValidInterviewQuestion: true only when the candidate represents an actual interview question or interview task; otherwise false.
- questionType: the appropriate interview-question category when valid, otherwise null.
- difficulty: Easy, Medium, Hard, or null.
- topics: concise topics directly represented by the question; [] when invalid.
- questionDescription: concise explanation of what the valid question asks; null when invalid.
- confidence: 0.0-1.0 confidence in the validation/classification.

Never drop an input candidate. Invalid candidates must still have an output entry with isValidInterviewQuestion=false and null/empty metadata. Do not invent missing information.

CANDIDATE_QUESTIONS_JSON:
{{questions_json}}
